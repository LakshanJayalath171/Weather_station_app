import { Text, View } from "react-native";

const sign_in = () => {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-2xl font-bold">sign_in</Text>
    </View>
  );
};

export default sign_in;
